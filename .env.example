# Copy this file to .env and fill in your own values. Never commit .env.
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=relieflink
DB_PORT=3306
SESSION_SECRET=
PORT=3000

# Task #2 — Media Upload Service (API Gateway + Lambda + S3)
# Invoke URL of the presign API, including the route path.
UPLOAD_API_URL=
# Bucket the presign Lambda writes to (for reference/documentation).
MEDIA_BUCKET=
